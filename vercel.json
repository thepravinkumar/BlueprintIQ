
---

## **8️⃣ `vercel.json` (Vercel Deployment Config)**
This file tells **Vercel** how to deploy the app.

```json
{
  "version": 2,
  "builds": [
    { "src": "src/main.py", "use": "vercel-python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/main.py" }
  ]
}
